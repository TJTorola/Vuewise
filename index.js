const icon        = require('~components/icon/basic'),
      workingIcon = require('~components/icon/working');

const box    = require('~components/box/basic'),
      button = require('~components/button/basic'),
      input  = require('~components/input/basic');

module.exports = {
  icon,
  workingIcon,
  box,
  button,
  input
};
